apiVersion: apps/v1
kind: Deployment
metadata:
   name: mydep
spec:
  template:
    metadata:
      name: mypod
      labels:
        app: web
    spec:
      containers:
        - name: nginx
          image: nginx:latest
  replicas: 1
  selector:
    matchLabels:
      app: web
